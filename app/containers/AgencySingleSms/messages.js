/*
 * AgencySingleSms Messages
 *
 * This contains all the text for the AgencySingleSms container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.AgencySingleSms';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the AgencySingleSms container!',
  },
});
