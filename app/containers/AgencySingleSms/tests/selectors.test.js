// import { selectAgencySingleSmsDomain } from '../selectors';

describe('selectAgencySingleSmsDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
