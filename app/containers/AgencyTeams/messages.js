/*
 * AgencyTeams Messages
 *
 * This contains all the text for the AgencyTeams container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.AgencyTeams';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the AgencyTeams container!',
  },
});
