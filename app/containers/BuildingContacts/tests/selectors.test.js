// import { selectBuildingContactsDomain } from '../selectors';

describe('selectBuildingContactsDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
