/*
 * BuildingContacts Messages
 *
 * This contains all the text for the BuildingContacts container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.BuildingContacts';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the BuildingContacts container!',
  },
});
