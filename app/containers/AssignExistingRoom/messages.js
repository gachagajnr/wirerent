/*
 * AssignExistingRoom Messages
 *
 * This contains all the text for the AssignExistingRoom container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.AssignExistingRoom';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the AssignExistingRoom container!',
  },
});
