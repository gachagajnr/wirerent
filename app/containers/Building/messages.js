/*
 * Building Messages
 *
 * This contains all the text for the Building container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.Building';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the Building container!',
  },
});
