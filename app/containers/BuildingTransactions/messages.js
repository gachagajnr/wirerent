/*
 * BuildingTransactions Messages
 *
 * This contains all the text for the BuildingTransactions container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.BuildingTransactions';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the BuildingTransactions container!',
  },
});
