// import { selectBuildingTransactionsDomain } from '../selectors';

describe('selectBuildingTransactionsDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
