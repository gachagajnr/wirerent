/*
 * AddNewBuildingNotice Messages
 *
 * This contains all the text for the AddNewBuildingNotice container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.AddNewBuildingNotice';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the AddNewBuildingNotice container!',
  },
});
