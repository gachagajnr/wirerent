// import { selectAddNewBuildingNoticeDomain } from '../selectors';

describe('selectAddNewBuildingNoticeDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
