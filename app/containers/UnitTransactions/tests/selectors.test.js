// import { selectUnitTransactionsDomain } from '../selectors';

describe('selectUnitTransactionsDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
