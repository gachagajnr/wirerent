/*
 * UnitRequests Messages
 *
 * This contains all the text for the UnitRequests container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.UnitRequests';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the UnitRequests container!',
  },
});
