/*
 * AddRequests Messages
 *
 * This contains all the text for the AddRequests container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.AddRequests';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the AddRequests container!',
  },
});
