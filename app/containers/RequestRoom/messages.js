/*
 * RequestRoom Messages
 *
 * This contains all the text for the RequestRoom container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.RequestRoom';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the RequestRoom container!',
  },
});
