/*
 * Agency Messages
 *
 * This contains all the text for the Agency container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.Agency';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the Agency container!',
  },
});
