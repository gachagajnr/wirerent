/*
 * BuildingNotices Messages
 *
 * This contains all the text for the BuildingNotices container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.BuildingNotices';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the BuildingNotices container!',
  },
});
