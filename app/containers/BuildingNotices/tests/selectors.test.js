// import { selectBuildingNoticesDomain } from '../selectors';

describe('selectBuildingNoticesDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
