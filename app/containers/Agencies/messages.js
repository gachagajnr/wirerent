/*
 * Agencies Messages
 *
 * This contains all the text for the Agencies container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.Agencies';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the Agencies container!',
  },
});
