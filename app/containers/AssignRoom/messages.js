/*
 * AssignRoom Messages
 *
 * This contains all the text for the AssignRoom container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.AssignRoom';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the AssignRoom container!',
  },
});
