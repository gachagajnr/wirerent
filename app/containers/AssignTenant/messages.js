/*
 * AssignTenant Messages
 *
 * This contains all the text for the AssignTenant container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.AssignTenant';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the AssignTenant container!',
  },
});
