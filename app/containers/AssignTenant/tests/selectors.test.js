// import { selectAssignTenantDomain } from '../selectors';

describe('selectAssignTenantDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
