/*
 * BuildingAdmins Messages
 *
 * This contains all the text for the BuildingAdmins container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.BuildingAdmins';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the BuildingAdmins container!',
  },
});
