// import { selectBuildingAdminsDomain } from '../selectors';

describe('selectBuildingAdminsDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
