/*
 * Unit Messages
 *
 * This contains all the text for the Unit container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.Unit';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the Unit container!',
  },
});
