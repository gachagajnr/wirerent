/*
 * BuildingUnits Messages
 *
 * This contains all the text for the BuildingUnits container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.BuildingUnits';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the BuildingUnits container!',
  },
});
