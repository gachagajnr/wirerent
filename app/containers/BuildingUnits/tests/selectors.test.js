// import { selectBuildingUnitsDomain } from '../selectors';

describe('selectBuildingUnitsDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
