// import { selectAddUnitTransactionDomain } from '../selectors';

describe('selectAddUnitTransactionDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
