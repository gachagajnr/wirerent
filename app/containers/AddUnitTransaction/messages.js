/*
 * AddUnitTransaction Messages
 *
 * This contains all the text for the AddUnitTransaction container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.AddUnitTransaction';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the AddUnitTransaction container!',
  },
});
