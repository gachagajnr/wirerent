/*
 * NewUnit Messages
 *
 * This contains all the text for the NewUnit container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.NewUnit';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the NewUnit container!',
  },
});
