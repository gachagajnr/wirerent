// import { selectAgencyProfileDomain } from '../selectors';

describe('selectAgencyProfileDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
