/*
 * AgencyProfile Messages
 *
 * This contains all the text for the AgencyProfile container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.AgencyProfile';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the AgencyProfile container!',
  },
});
