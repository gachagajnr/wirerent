/*
 * AddBuildingLocation Messages
 *
 * This contains all the text for the AddBuildingLocation container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.AddBuildingLocation';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the AddBuildingLocation container!',
  },
});
