/*
 * QuickView Messages
 *
 * This contains all the text for the QuickView container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.QuickView';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the QuickView container!',
  },
});
