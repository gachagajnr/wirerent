/*
 * BuildingTenants Messages
 *
 * This contains all the text for the BuildingTenants container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.BuildingTenants';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the BuildingTenants container!',
  },
});
