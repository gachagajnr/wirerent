// import { selectBuildingTenantsDomain } from '../selectors';

describe('selectBuildingTenantsDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
