/*
 * Units Messages
 *
 * This contains all the text for the Units container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.Units';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the Units container!',
  },
});
