// import { selectBuildingRequestsDomain } from '../selectors';

describe('selectBuildingRequestsDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
