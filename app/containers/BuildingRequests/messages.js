/*
 * BuildingRequests Messages
 *
 * This contains all the text for the BuildingRequests container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.BuildingRequests';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the BuildingRequests container!',
  },
});
