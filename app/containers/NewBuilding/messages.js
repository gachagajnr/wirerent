/*
 * NewBuilding Messages
 *
 * This contains all the text for the NewBuilding container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.NewBuilding';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the NewBuilding container!',
  },
});
