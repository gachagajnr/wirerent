/*
 * BuildingPaymentInfo Messages
 *
 * This contains all the text for the BuildingPaymentInfo container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.BuildingPaymentInfo';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the BuildingPaymentInfo container!',
  },
});
