// import { selectBuildingPaymentInfoDomain } from '../selectors';

describe('selectBuildingPaymentInfoDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
