/*
 * BuildingProfile Messages
 *
 * This contains all the text for the BuildingProfile container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.BuildingProfile';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the BuildingProfile container!',
  },
});
