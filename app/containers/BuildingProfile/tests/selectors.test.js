// import { selectBuildingProfileDomain } from '../selectors';

describe('selectBuildingProfileDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
