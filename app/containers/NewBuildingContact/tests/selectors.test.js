// import { selectNewBuildingContactDomain } from '../selectors';

describe('selectNewBuildingContactDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
