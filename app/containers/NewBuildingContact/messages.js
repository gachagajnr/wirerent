/*
 * NewBuildingContact Messages
 *
 * This contains all the text for the NewBuildingContact container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.NewBuildingContact';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the NewBuildingContact container!',
  },
});
