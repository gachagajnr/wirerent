// import { selectNewBuildingAdminDomain } from '../selectors';

describe('selectNewBuildingAdminDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
