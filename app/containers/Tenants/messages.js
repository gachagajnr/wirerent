/*
 * Tenants Messages
 *
 * This contains all the text for the Tenants container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.Tenants';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the Tenants container!',
  },
});
