/*
 * RequestDetails Messages
 *
 * This contains all the text for the RequestDetails container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.RequestDetails';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the RequestDetails container!',
  },
});
