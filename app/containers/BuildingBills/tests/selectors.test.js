// import { selectBuildingBillsDomain } from '../selectors';

describe('selectBuildingBillsDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
