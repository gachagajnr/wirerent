// import { selectAddBuildingPaymentInfoDomain } from '../selectors';

describe('selectAddBuildingPaymentInfoDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
